import { Typography } from '../components/Typography';

const Home = () => {
  return (
    <div>
      <Typography size="16" color="blue">
        인트로 페이지입니다.
      </Typography>
    </div>
  );
};

export default Home;
